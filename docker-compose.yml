version: "3.9"

services:
  log-classifier:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./models:/app/models
      - ./training:/app/training
      - ./evaluation:/app/evaluation
    restart: unless-stopped
